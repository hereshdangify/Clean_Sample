import 'package:flutter/material.dart';

class MyAuthenticationButton extends StatelessWidget {
  const MyAuthenticationButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
