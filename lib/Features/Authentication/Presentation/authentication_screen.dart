import 'package:flutter/material.dart';
import 'package:gc/Features/Authentication/Presentation/Controller/authentication_controller.dart';
import 'package:get/get.dart';

class AuthenticationScreen extends GetView<AuthenticationController> {
  const AuthenticationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    //Textfields and buttons for registering user
    return const Scaffold();
  }
}
